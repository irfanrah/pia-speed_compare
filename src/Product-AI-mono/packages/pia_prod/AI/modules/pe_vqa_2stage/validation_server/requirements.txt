# vLLM 베이스 이미지에 포함되지 않은 추가 의존성만 명시.
# fastapi, httpx, uvicorn, pydantic은 vLLM 이미지에 포함되어 있다.
#
# MESSAGING_BACKEND=rabbitmq 사용처(KTT 등)는 pika 필요.
# MESSAGING_BACKEND=kafka 사용처(ES MACS 등)는 confluent-kafka(Producer만) + fastavro 필요.
# Schema Registry 미사용 (ES backend가 fastavro.schemaless_reader로 디코드하므로 와이어 포맷 매칭).
pika>=1.3.0
boto3>=1.35.0
redis>=5.0.0
confluent-kafka>=2.3.0
fastavro>=1.9.0
